package com.example.bookstore.repository;

import com.example.bookstore.domain.PaymentTransaction;
import com.example.bookstore.repository.common.AbstractRepository;

public interface PaymentTransactionRepository extends AbstractRepository<PaymentTransaction> {
}